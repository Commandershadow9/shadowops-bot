{
  "type": "object",
  "properties": {
    "title": { "type": "string" },
    "summary": { "type": "string" },
    "changes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["feature", "fix", "improvement", "breaking"] },
          "description": { "type": "string" }
        },
        "required": ["type", "description"],
        "additionalProperties": false
      }
    },
    "version": { "type": "string" },
    "language": { "type": "string", "enum": ["de", "en"] }
  },
  "required": ["title", "summary", "changes", "version", "language"],
  "additionalProperties": false
}
