# Discord Bot Framework
# Allow minor/patch updates but pin major versions for stability
discord.py>=2.3.2,<3.0.0
aiohttp>=3.9.1,<4.0.0

# Configuration
PyYAML>=6.0.1,<7.0.0
python-dotenv>=1.0.1,<2.0.0

# Database
aiosqlite>=0.20.0,<1.0.0
# Postgres-Treiber: asyncpg (async, primaer fuer security_engine + jules_state)
# psycopg2-binary (sync, fuer knowledge_base.py)
# Beide werden seit Phase 5c+ in mehreren Modulen genutzt — ohne sie schlaegt
# pytest collection fehl mit ModuleNotFoundError (CI-Bug 2026-04-27 vor PR).
asyncpg>=0.30.0,<1.0.0
psycopg2-binary>=2.9.9,<3.0.0

# JSON-Schema-Validierung (ai_engine.py, test_jules_review_schema.py)
jsonschema>=4.20.0,<5.0.0

# Utilities
python-dateutil>=2.8.2,<3.0.0
pytz>=2024.1

# Logging
coloredlogs>=15.0.1,<16.0.0

# AI Services
# Allow minor updates for bug fixes and features, but pin major versions
openai>=1.54.0,<2.0.0
anthropic>=0.39.0,<1.0.0
httpx>=0.27.0,<0.28  # Pin to <0.28 for AI library compatibility (proxies parameter removed in 0.28)
